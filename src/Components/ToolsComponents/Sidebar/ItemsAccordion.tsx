import React from "react";

type INav = { title: string; to: string };
type Props = {
  nav: INav[];
};

const ItemsAccordion = (props: Props) => {
  return <div></div>;
};

export default ItemsAccordion;
